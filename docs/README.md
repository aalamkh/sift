# docs

Drop a screen recording of the app here named **`demo.gif`** — the root
`README.md` references it at `docs/demo.gif`.

Suggested capture: load the **B2B SaaS** sample → **Analyze feedback** →
edit a RICE input to show live re-bucketing → **Generate rationale** →
**Export Markdown**.
